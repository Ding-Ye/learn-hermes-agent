---
name: summarize
description: 把 ${HERMES_SKILL_INPUT} 指定的文件做一份结构化摘要。
---

任务：把下面这份内容做一份结构化摘要。

输入文件路径：${HERMES_SKILL_INPUT}

输入内容（自动注入）：
```
`cat "${HERMES_SKILL_INPUT}"`
```

请按以下结构输出，每节不超过两句话：

1. **核心结论**：作者要传达的最终观点。
2. **关键证据**：3 条以内，每条一行。
3. **未解决的问题**：如果有，列 1-2 个；没有就写"无"。

不要复述原文。
